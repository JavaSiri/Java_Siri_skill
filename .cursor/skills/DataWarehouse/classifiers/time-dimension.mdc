---
description: 时间维度分类器 - 判断需求应按日/周/月哪个粒度进行统计
---

# 时间维度分类器

## 职责

判断需求应按哪个**时间粒度**进行观察和统计。

> 仅对「自然周期型」需求进行时间维度判断。「切片累积型」以 MOB（账龄月）作为观察粒度，不走此分类器。

## 统一术语

| 术语 | 含义 |
|------|------|
| 自然时间 | 业务事件发生的自然时间，如申请日期、放款日期、还款日期 |
| 时间维度 | 观察数据的粒度（日/周/月），由业务需求决定而非字段命名 |
| 时间字段 | 用于切分时间范围的字段，如 `*_date`、`*_week`、`*_month` |

## 分类维度

| 维度 | 自然时间示例 | 说明 |
|------|------------|------|
| **日维** | 申请日期、放款日期、支用日期 | 统计某一天的事件汇总 |
| **周维** | 自然周（周一~周日） | 统计某一自然周的事件汇总 |
| **月维** | 申请月、放款月、应还款月 | 统计某一自然月的事件汇总 |

## 判断规则

### 规则一：时间维度由业务需求决定

**核心问题：用户想按什么粒度来观察这个指标？**

同一类业务事件可以有多个观察粒度的版本：

| 同一业务事件的不同粒度 | 示例 |
|----------------------|------|
| 日维 | 按申请日期统计每日申请量 |
| 月维 | 按申请月统计每月申请量 |

**字段名中的时间后缀（`*_date`、`*_month`）是提示，不是决定依据。** 关键要看业务需求中想用什么粒度观察。

### 规则二：区分自然时间与切片维度

- **自然时间**：业务事件发生的日期字段（`apply_date`、`loan_date`），用于确定时间维度
- **切片维度**：Cohort 划分依据（`credit_month`、`first_loan_month`），用于 Cohort 归属

两者可能相同字段，也可能不同。示例：

| 需求 | 自然时间 | 切片维度 |
|------|---------|---------|
| 按申请月统计当月放款金额 | `apply_month` | 无（自然周期型） |
| 按首支月统计放款金额 | `loan_month` | `first_loan_month` |

### 规则三：字段后缀与维度的对应关系（辅助参考）

| 字段后缀示例 | 常见含义 | 维度判断 |
|------------|---------|---------|
| `*_date` | 申请日期、放款日期 | 日维 |
| `*_week` | 自然周 | 周维 |
| `*_month` | 申请月、放款月、应还款月 | 月维（需结合语义判断是自然时间还是切片维度） |

> 警告：`*_month` 后缀既可能是自然时间（申请月），也可能是切片维度（首支月）。必须结合业务语义判断，**禁止仅根据后缀判断维度**。

## 判断逻辑

```
Step 1: 确定自然时间字段（业务事件发生的时间）
Step 2: 根据业务需求确定观察粒度（日/周/月）
Step 3: 输出时间维度
```

## 输出格式

```
分类结果：时间维度 = [日维 / 周维 / 月维]
自然时间字段：[字段名]
判断依据：[业务原因]
```

## 典型场景

| 场景 | 自然时间 | 时间维度 | 判断依据 |
|------|---------|---------|---------|
| 统计每日申请量 | `apply_date` | 日维 | 需求明确要求按"日"观察 |
| 统计每月放款金额 | `loan_month` | 月维 | 需求明确要求按"月"观察 |
| 按周观察转化率 | `event_week` | 周维 | 需求要求按自然周粒度汇总 |
