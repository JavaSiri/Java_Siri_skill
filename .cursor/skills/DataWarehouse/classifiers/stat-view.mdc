---
description: 统计视角分类器 - 判断需求属于切片累积型还是自然周期型
---

# 统计视角分类器

## 职责

判断需求属于**切片累积型**还是**自然周期型**。

> 这是最核心的分类，决定了 ETL 开发的基本逻辑走向。

## 统一术语

| 术语 | 含义 |
|------|------|
| 自然周期型 | 统计某时间切面下的事件汇总（笔数/金额/转化率），不追踪样本历史表现 |
| 切片累积型 | 追踪同一批样本（Cohort）从切片维度起点到当前时点的累计表现 |
| 切片维度 | 划定"某批样本"的起点，如首支月、应还款月、授信月 |
| 自然时间 | 业务事件发生的自然时间，如申请日期、放款日期 |
| Cohort | 同一批次样本，按某个时间维度（切片维度）划分 |

## 核心判断标准

**判断依据：业务统计视角，而非字段命名。**

核心问题：**这批指标是在观察"某时间切面发生了什么事件"，还是在追踪"同一批样本从某个起点到现在的累计表现"？**

## 两种类型的本质区别

| | 自然周期型 | 切片累积型 |
|---|---|---|
| **观察对象** | 某时间切面下的事件 | 同一批样本（Cohort）的累计行为 |
| **维度** | 自然时间（日/月） | 切片维度（Cohort 划分） + MOB（账龄月） |
| **典型指标** | 当期申请笔数、当期放款金额、当期转化率 | Vintage 逾期率、催回率、累计支用次数 |
| **样本追踪** | 不追踪 | 追踪同一批样本从起点到当前的表现 |
| **时间切片** | 自然时间切面（某日/某月） | Cohort 切面（首支月/应还款月） |

## 判断逻辑

### 识别切片累积型

满足以下**任意一条**，倾向切片累积型：

1. **指标语义为累计值**：追踪同一批样本的累计表现
   - 示例：`mob1_ovd_num`、`vintage_rate`、`total_loan_cnt`、`cumulative_repay_amt`

2. **业务场景是 Cohort 分析**：按某个时间维度切片后追踪该批样本
   - 示例：Vintage 逾期率（按首支月切片）、催回率（按应还款月切片）

3. **核心问题是"这批人怎么样了"**：而不是"某天/某月发生了多少"

**关键问题：这批指标是否有一个"起点"（切片维度），然后追踪该批样本从起点到当前时点的表现？**

- 是 → 切片累积型
- 否 → 自然周期型

### 识别自然周期型

满足以下**全部条件**，倾向自然周期型：

1. 指标语义为**当期汇总值**，不包含累计追踪
   - 示例：`apply_cnt`（当期申请笔数）、`loan_amt`（当期放款金额）、`pass_rate`（当期通过率）

2. 观察粒度是**自然时间切面**（某日/某周/某月的事件汇总），而非 Cohort 表现

3. 核心问题是"某时间切面发生了什么"，而不是"某批人怎么样了"

## 常见误区警示

| 误区 | 正确认知 |
|------|---------|
| `*_month` 后缀 = 自然周期型 | 错！`credit_month` 可能是切片维度（首支月/应还款月），也可能是自然时间（申请月） |
| 表名含 "monthly" = 月维自然周期型 | 错！催回表表名含 monthly 但字段是应还款月，属于切片累积型 |
| UNION ALL = 切片累积型 | 错！两种类型都可能用 UNION ALL |
| 需求描述"统计某时间段内汇总" = 自然周期型 | 不一定！可能是输出形式，需结合指标字段判断 |
| 按字段名判断类型 | 错！必须按业务统计视角判断：是否追踪样本从起点到当前时点的累计表现 |
| `mob*` 字段 = 切片累积型 | 错！`mob*` 是账龄口径描述，仅在切片累积型场景下使用，但本身不决定类型 |

## 输出格式

```
分类结果：统计视角 = [自然周期型 / 切片累积型]
切片维度：[字段名 / 无]
时间维度：[日维 / 周维 / 月维]（仅自然周期型需填写）
判断依据：[业务语义描述]
```
