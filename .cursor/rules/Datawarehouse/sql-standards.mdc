---
description: SQL 通用规范 - CTE、字段、关联过滤、data_date 等基础规则
globs: "**/*.sql"
---

# SQL 通用规范

所有 SQL 脚本必须遵循以下规范。

## 统一术语

| 术语 | 含义 |
|------|------|
| T | 当前执行的目标业务日期，正常执行时=prebizdate，回刷时=回刷目标日期 |
| T-1 | T 的前一天业务日期 |
| T-2 | T 的前两天业务日期 |
| 幂等性 | 重复执行产生相同结果，防止数据重复或丢失 |
| 分区策略 | 数据按时间维度分区存储，便于增量处理和回刷 |

## CTE 命名规范

- CTE 命名必须统一为：`cte<序号>_<语义名>`
  - 示例：`cte0_refresh_date_scope`、`cte1_history_weeks`、`cte5_refresh_month_new`
- 禁止使用不带 `cte<序号>_` 前缀的 CTE 名称
- `cte0` 保留为运行参数/日期窗口 CTE，推荐固定命名：`cte0_refresh_date_scope`

## SELECT / INSERT 书写规范

### SELECT 书写规范

- 每个字段必须独占一行，禁止多个字段写在同一行
- CTE 内及主查询中，禁止使用 `SELECT *` 或 `<alias>.*`，必须显式列出字段
- 字段显式列出时，需与目标字段顺序保持一致（尤其是 INSERT 场景）

### INSERT 书写规范（强制）

**INSERT INTO 后必须显式列出所有目标字段，禁止省略列列表。**

| 场景 | 是否必须写列列表 | 说明 |
|------|-----------------|------|
| `INSERT INTO <table>` 无列列表 | ❌ 禁止 | 依赖表定义顺序，隐式映射易出错 |
| `INSERT INTO <table> (col1, col2, ...)` 带显式列列表 | ✅ 必须 | 显式声明目标字段，顺序与 SELECT 对齐 |

**INSERT 列列表规则：**

1. **每行一个字段**：`INSERT INTO ... (field1, field2, ...)` 格式改为每行一个字段，换行书写
2. **字段顺序与 SELECT 列表严格对齐**：INSERT 声明的第 N 个字段 = SELECT 的第 N 个字段
3. **ds 字段处理**：PostgreSQL 需写 `ds` 列，Hive 静态分区不写 `ds` 列（由 PARTITION 声明），动态分区 SELECT 最后一列为 `ds` 并以别名形式出现

**正确示例：**

```sql
INSERT INTO schema.target_table
(
    data_date
    ,month_start_date
    ,diversion_type
    ,customer_count
    ,ds
)
SELECT  data_date
        ,month_start_date
        ,diversion_type
        ,SUM(customer_count) AS customer_count
        ,'{prebizdate}' AS ds
FROM    cteN_result
;
```

**错误示例（禁止）：**

```sql
-- ❌ 错误：INSERT INTO 后省略列列表
INSERT INTO schema.target_table
SELECT  data_date
        ,month_start_date
        ,...
;

-- ❌ 错误：INSERT 列列表中多字段合并在同一行
INSERT INTO schema.target_table
(   data_date
    ,month_start_date, diversion_type
    ,customer_count
)
SELECT ...
;
```

## 金额字段处理

根据**目标层**和**源表层**决定处理规则：

| 目标层 | 源表层 | 处理规则 | 说明 |
|--------|--------|----------|------|
| DWS 层 | - | 不处理 | 保持上游口径 |
| ADS 层 | DWS 层 | `/100.0` 分转元 | DWS 层金额单位为分，需转换 |
| ADS 层 | ADS 层 | 不处理 | 上游已为元，避免重复转换 |
| 任意层 | 任意层 | **转换仅在源表读取型 CTE 完成，后续 CTE 禁止再做 `/100`** | 前置原则，防止双重转换 |
| 非金额字段 | - | 不适用 | 无需处理 |

**金额转换前置原则**：金额单位转换应在**源表读取型 CTE** 中完成（距离数据源最近的 CTE），后续 CTE 不应对金额字段再做 `/100` 处理。

**正确示例：**

```sql
-- ✅ 正确：金额转换在源表读取型 CTE 中完成
,cte1_dws_source AS (
    SELECT  customer_sk
            ,loan_amt / 100.0 AS loan_amt          -- 在源表读取型 CTE 中完成分转元
            ,repay_amt / 100.0 AS repay_amt
    FROM    dws_xxx
    WHERE   ds = '{prebizdate}'
)
-- 后续 CTE 直接引用已转换的字段，不再做 /100
,cte2_agg AS (
    SELECT  customer_sk
            ,SUM(loan_amt) AS total_loan_amt       -- 直接使用，无需再转换
            ,SUM(repay_amt) AS total_repay_amt
    FROM    cte1_dws_source
    GROUP BY customer_sk
)
```

**错误示例（禁止）：**

```sql
-- ❌ 错误：金额转换分散在多个 CTE 中
,cte1_dws_source AS (
    SELECT  customer_sk
            ,loan_amt                              -- 未转换
            ,repay_amt                             -- 未转换
    FROM    dws_xxx
    WHERE   ds = '{prebizdate}'
)
,cte2_agg AS (
    SELECT  customer_sk
            ,SUM(loan_amt / 100.0) AS total_loan_amt    -- ❌ 错误：不应在此处转换
            ,SUM(repay_amt) AS total_repay_amt
    FROM    cte1_dws_source
    GROUP BY customer_sk
)
```

**ADS→ADS 场景示例：**

```sql
-- ✅ 正确：上游已是 ADS 层，金额已是元，不做 /100 转换
,cte3_ads_source AS (
    SELECT  decision_date
            ,decision_amount AS decision_amount    -- 直接透传，不转换
    FROM    ads_node_decision_daily_count_di
    WHERE   ds = '{prebizdate}'
)
```

## 关联过滤原则

根据**关联类型**决定过滤条件位置：

| 关联类型 | 过滤条件位置 | 说明 |
|----------|-------------|------|
| LEFT JOIN（左表） | WHERE 子句 | 左表在 FROM 后，为主表 |
| LEFT JOIN（右表） | JOIN ... ON 中 | 右表为从表 |
| RIGHT JOIN（左表） | JOIN ... ON 中 | 左表为从表 |
| RIGHT JOIN（右表） | WHERE 子句 | 右表在 FROM 后，为主表 |
| INNER JOIN | 任一位置均可 | 两侧都非必须保留 |

**关键约束**：禁止将从表过滤条件写入 WHERE，避免 LEFT JOIN 被意外收敛为 INNER JOIN。

## 分区裁剪规则（强制）

所有分区字段过滤必须使用硬编码常量，不得通过 `CROSS JOIN` 引用 CTE 变量。

### 约束说明

| 字段类型 | 使用方式 | 示例 |
|----------|----------|------|
| `ds` 分区字段 | 硬编码常量（不得 CROSS JOIN 引用） | `WHERE t.ds = '{prebizdate}'` |
| 非分区字段 | 可通过 `CROSS JOIN cte0_refresh_date_scope rds` 引用 | `WHERE t.business_date >= rds.win_start_ds` |

> **原因**：`ds` 分区字段若通过 CROSS JOIN 传递，优化器无法稳定触发分区裁剪；非分区字段无此限制。

### CTE0 维护原则

`cte0_refresh_date_scope` 是**标准参数列表**，用于统一维护日期变量名称和语义：

- 即使某些字段当前脚本未使用，也应保留定义，以确保团队代码风格一致
- 代码审查中，**不应将「CTE0 字段未被使用」作为问题提出**
- 如需调整日期计算逻辑，只需在 cte0 定义处修改一处即可
- CTE0 具体实现：参见各数据库对应 skill 中的「CTE0 运行参数实现」章节

## data_date 字段规范

`data_date` 是"数据截止日期"字段，含义为：该行聚合结果是截止到哪一天的业务数据。

| 场景 | 取值规则 | 说明 |
|------|----------|------|
| 日维 | `data_date = 业务日期字段` | 每行只有一天，截止日期=业务日期 |
| 月/周维 | `data_date = MAX(业务日期字段)` | 取窗口内最大业务日期 |
| 历史继承行 | 保持原值不变 | 继承历史快照 |

**禁止从上游继承 data_date**，必须从实际业务日期字段派生。

## 幂等性约束

重复执行应产生相同结果：

| 约束项 | 说明 |
|--------|------|
| 防止重复写入 | 同一分区/时间段的数据应被覆盖而非追加 |
| 防止数据丢失 | 回刷历史数据时不应影响其他分区 |
| 原子性操作 | 写入操作应保证完整性 |

## 窗口计算规范

### 自然周期型（授信相关）

**适用场景**：日维、月维、周维统计表，上游数据延迟为 T-1（当天数据次日可用）

**回刷窗口**：读上游 `[week_start(T-2), T]` / `[month_start(T-2), T]` / `[T-2, T]`

**原因**：需要覆盖迟到数据，确保窗口内所有事件被统计

| 维度 | 回刷窗口（T=目标业务日期） | 写入 ds 取值 |
|------|---------------------------|-------------|
| 日维 | `[T-2, T]` | 取上游业务日期字段值 |
| 周维 | `[week_start(T-2), T]` | `T`（当前计算的最大业务时间） |
| 月维 | `[month_start(T-2), T]` | `T`（当前计算的最大业务时间） |

**说明**：
- 周维/月维：每日全量快照，写入 ds 取当前计算的最大业务时间（授信取 T，借款/贷后取 T-1），历史月份数据从上一分区继承
- 日维：动态分区，写入 ds 取上游业务日期字段值

### 自然周期型（借款/贷后相关）

**适用场景**：日维、月维、周维统计表，上游数据延迟为 T-2（当天数据隔日可用）

**回刷窗口**：读上游 `[week_start(T-3), T-1]` / `[month_start(T-3), T-1]` / `[T-3, T-1]`

**原因**：需要覆盖迟到数据，确保窗口内所有事件被统计

| 维度 | 回刷窗口（T=目标业务日期） | 写入 ds 取值 |
|------|---------------------------|-------------|
| 日维 | `[T-3, T-1]` | 取上游业务日期字段值 |
| 周维 | `[week_start(T-3), T-1]` | `T-1`（当前计算的最大业务时间） |
| 月维 | `[month_start(T-3), T-1]` | `T-1`（当前计算的最大业务时间） |

**说明**：
- 周维/月维：每日全量快照，写入 ds 取当前计算的最大业务时间（授信取 T，借款/贷后取 T-1），历史月份数据从上一分区继承
- 日维：动态分区，写入 ds 取上游业务日期字段值

### 切片累积型（Vintage、催回等）

- 观测日期 ≤ T-1，无累积窗口概念
- 分区策略：数据截止时间分区（ds = 上游最大业务日期）

## 分区策略选择

根据**指标类型**选择分区策略，核心判断标准：**"这个指标是否需要完整的历史事件序列？"**

### 指标类型与分区策略对照

| 指标类型 | 典型场景 | 分区策略 | 原因 |
|---------|---------|---------|------|
| 事件序列型 | 漏斗、留存、路径分析 | 业务时间分区（`ds = 业务日期`） | 需要追溯完整事件链，支持按业务时间范围分区裁剪 |
| 期末快照型 | 余额、人数、Vintage | 数据截止时间分区（`ds = 上游最大业务日期`） | 关注"截止到T"的累计值，分区值表示数据截止的业务时点 |
| 占比趋势型 | 转化率、占比、均值 | 数据截止时间分区（`ds = 上游最大业务日期`） | 分子分母同周期，比值稳定，无需业务时间追溯 |

### 分区策略决策树

```
指标是否需要完整的历史事件序列？
├── 是（漏斗、留存等）→ 业务时间分区（ds = 业务日期）
└── 否（Vintage、余额等）→ 数据截止时间分区（ds = 上游最大业务日期）
```

### 实现示例

> **说明**：以下为概念性写法，用于说明分区策略的业务语义。各数据库的具体语法参见对应 skill。

**业务时间分区（漏斗场景）**：

```
写入逻辑：ds 取业务日期（如 20260505），按业务时间分区裁剪
SQL 语义：目标分区 = 业务日期当天
```

**数据截止时间分区（Vintage 场景）**：

```
写入逻辑：ds 取上游最大业务日期（T-1），表示数据的业务截止时点
SQL 语义：目标分区 = 上游最大业务日期
```

### 设计原则

分区策略服务于**业务语义**而非一刀切。规范允许根据指标类型灵活选择，以在查询性能（分区裁剪）与实现一致性之间取得平衡。

## SQL 脚本注释规范

适用于 `exe_sql/` 目录下的 ETL 脚本（PostgreSQL / Hive DML 语句）。

### 文件头部注释（强制）

SQL 脚本的文件头部必须包含以下注释块：

```sql
--********************************************************************--
-- author: <作者名>
-- create time: <yyyy-MM-dd HH:mm:ss>
-- description: <表描述>
--********************************************************************--
-- 功能说明：
--   1) <功能点1>
--   2) <功能点2>
--   3) <功能点3>
--
-- 数据来源与口径：
--   1) 数据来源：<来源表>
--   2) 分流维度：<关联逻辑>
--   3) 金额单位：<元/分说明>
--   4) 过滤范围：<分区过滤逻辑>
--
-- 完整字段格式说明：
--   1) data_date             VARCHAR(8)，yyyyMMdd，数据截止日期
--   2) diversion_type       VARCHAR(10)，分流类型 1:华通/晋商 2:嵩海
--   3) credit_month         VARCHAR(7)，yyyy-MM，授信月
--   4) credit_customer_count BIGINT，授信客户数
--   5) ga_cust_mob0~24      BIGINT，GA客户数（mob0~24）
--   6) ds                   VARCHAR(8)，yyyyMMdd，业务日期分区
--********************************************************************--
```

### CTE 注释块（强制）

#### 位置规范

**注释块必须写在 `AS (` 之前，而非括号内部。**

**正确示例：**

```sql
-- ============================================================================
-- CTE1：<该 CTE 的一句话功能>
-- 说明：
--   1) 数据来源：<来源表>
--   2) 过滤条件：<过滤条件>
--   3) 口径：<关键计算口径>
--   4) 输出字段：<输出字段列表>
-- ============================================================================
-- 字段格式说明：
--   field1              VARCHAR，<格式> 示例：<sample> <含义>
--   field2              BIGINT，<格式> 示例：<sample> <含义>
,cte1_source_data AS (   -- ✅ 注释在 AS ( 之前
```

**错误示例（禁止）：**

```sql
,cte1_source_data AS (
    -- ============================================================================
    -- ❌ 错误：注释写在 AS ( 括号内部
    -- ============================================================================
    SELECT ...
)
```

#### 格式规范

**逐个 CTE 注释**使用如下结构：

```sql
-- ============================================================================
-- CTE0：运行参数定义
-- 说明：
--   1) <功能点1>
--   2) <功能点2>
--   3) <功能点3>
-- ============================================================================
-- 字段格式说明：
--   cur_ds              VARCHAR，yyyyMMdd 示例：20260402 当前业务日
--   yesterday_ds        VARCHAR，yyyyMMdd 示例：20260401 前一业务日
--   win_start_ds        VARCHAR，yyyyMMdd 示例：20260301 回刷窗口起始日
WITH cte0_refresh_date_scope AS (
    SELECT  '{prebizdate}' AS cur_ds
            ,TO_CHAR(TO_DATE('{prebizdate}', 'YYYYMMDD') - INTERVAL '1 day', 'YYYYMMDD') AS yesterday_ds
            ,TO_CHAR(DATE_TRUNC('month', TO_DATE('{prebizdate}', 'YYYYMMDD') - INTERVAL '2 day'), 'YYYYMMDD') AS win_start_ds
)

-- ============================================================================
-- CTE1：<该 CTE 的一句话功能>
-- 说明：
--   1) 数据来源：<来源表>
--   2) 过滤条件：<过滤条件>
--   3) 口径：<关键计算口径>
--   4) 输出字段：<输出字段列表>
-- ============================================================================
-- 字段格式说明：
--   field1              VARCHAR，<格式> 示例：<sample> <含义>
--   field2              BIGINT，<格式> 示例：<sample> <含义>
,cte1_source_data AS (
    SELECT  ...
)
```

#### 内容要求

**逐类 CTE 注释要求：**

- **CTE0（运行参数）**：必须写完整字段格式说明，字段名必须与 SQL 中真实别名完全一致
- **业务计算 CTE（如 CTE1/CTE2/CTE3...）**：说明数据来源（DWS/ADS 表名）、关键口径（如金额是否 `/100`、是否累计、是否继承历史）、过滤原则（按 `ds` 分区过滤回刷窗口）；字段格式说明至少覆盖输出关键日期字段（如 `data_date`、`week_start_date`、`month_start_date`）
- **FINAL 段**：说明写入策略（动态/静态分区）及覆盖范围（窗口分区或当日分区）

#### 字段格式说明模板

```
--   cur_ds              STRING，yyyyMMdd 示例：20260402 当前业务日
--   win_start_ds        STRING，yyyyMMdd 示例：20260331 回刷窗口起始日
--   yesterday_ds        STRING，yyyyMMdd 示例：20260401 前一业务日
```

#### 书写风格

- 中文全角标点统一（如"，"）
- 列表层级统一使用"1) 2) ..."
- 避免口语化描述，采用"来源/口径/范围/结果"四段式表达
- 同一文件内术语统一："回刷窗口""累计窗口""当前业务日""前一业务日""物理分区 ds"

---

## DDL 注释规范

适用于 `09-晋消上线/DDL/` 目录下的建表脚本，参见 `pgsql-standards.mdc`。

---

## 快速验收清单

> **审查方法**：对每条检查项，先提取代码中的实际值，再与正文章节中的标准参数逐一对比。
> **判定规则**：实际值与标准参数完全一致 → ✅；不一致 → ❌（需修复）。
>
> **规范出处**：正文「SQL 脚本注释规范」「窗口计算规范」「关联过滤原则」「SELECT 书写规范」等章节是唯一事实来源。验收清单仅负责提取规则和判定逻辑，不重复正文中的规则描述。

---

### SQL-1：窗口范围

**规范出处**：「窗口计算规范」章节。

**代码实际值提取规则**：

1. 根据业务语义确定指标类型（授信/借款+贷后/切片累积）
2. 根据时间维度（日/周/月）确定标准窗口
3. 扫描 CTE2 的 `WHERE ds BETWEEN ... AND ...` 或 `WHERE ds >= ... AND ds <= ...`，提取实际窗口边界
4. 确认上游 DWS 表的延迟类型（授信 T-1 / 借款贷后 T-2）

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 自然周期型（授信）实际窗口 = `[T-2, T]` / `[week_start(T-2), T]` / `[month_start(T-2), T]`，写入 ds 取 T | ✅ |
| 自然周期型（借款/贷后）实际窗口 = `[T-3, T-1]` / `[week_start(T-3), T-1]` / `[month_start(T-3), T-1]`，写入 ds 取 T-1 | ✅ |
| 切片累积型，观测日期 ≤ T-1，无累积窗口 | ✅ |
| 实际窗口边界与标准参数不一致 | ❌（列出实际窗口 vs 预期窗口） |

---

### SQL-2：金额口径

**规范出处**：「金额字段处理」章节。

**前置原则**：金额单位转换必须在**源表读取型 CTE**（距数据源最近的 CTE）中完成，后续 CTE 不得再对金额字段做 `/100` 处理。

**代码实际值提取规则**：

1. **按链路追踪**：从源表读取型 CTE 开始，沿数据流方向追踪每个金额字段的转换状态
2. **识别源表读取型 CTE**：CTE 的 FROM 子句直接引用 DWS/ADS 物理表（如 `FROM ttsp_it.dws_xxx`）的 CTE，标记为源表读取型
3. **记录转换状态**：在每个 CTE 中，扫描金额相关字段（包含 `amt`、`amount`、`balance`、`principal`、`interest` 等关键词），记录是否包含 `/100` 或 `/100.0`
4. **追踪下游引用**：检查后续 CTE 是否对同一字段再次做 `/100` 转换

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 源表读取型 CTE 中完成 `/100` 转换，后续 CTE 不再转换 | ✅ |
| 源表读取型 CTE 中未做 `/100` 转换，转换被推迟到后续 CTE | ❌（前置原则违反） |
| 后续 CTE 对已转换的金额字段再次做 `/100` 转换 | ❌（双重转换） |
| DWS 层有 `/100` 转换 | ❌（不应处理金额） |

**审查输出格式**：

```
CTE 链路追踪：
  cte2（源表读取型，DWS）→ 金额字段 X：/100.0 ✅
  cte5（聚合型，FROM cte2）→ 金额字段 X：直接引用 ✅

结论：[通过/不通过]
```

---

### SQL-3：历史继承

**规范出处**：「窗口计算规范」章节。

**代码实际值提取规则**：

1. 扫描 DML 部分，检查是否存在 UNION ALL
2. 检查 UNION ALL 前是否为窗口重算结果，后是否为历史分区继承
3. 检查历史继承的 WHERE 中是否有 `ds = T-1` + `month_start_date < win_start_ds`

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 月/周维包含 UNION ALL 历史继承 | ✅ |
| 切片累积型无 UNION ALL | ✅ |
| 日维无 UNION ALL | ✅ |
| 月/周维缺少历史继承 | ❌ |
| 日维包含不必要的 UNION ALL | ❌ |

---

### SQL-4：关联过滤位置

**规范出处**：「关联过滤原则」章节。

**代码实际值提取规则**：

1. 扫描所有 `LEFT JOIN` / `RIGHT JOIN`
2. 检查从表过滤条件是否写在对应 `ON` 子句中
3. 检查是否存在 `JOIN` 后续的独立 `WHERE` 条件引用了从表字段

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 从表过滤在 `ON` 中 | ✅ |
| 从表过滤在 `WHERE` 中（LEFT JOIN 被意外收敛为 INNER JOIN） | ❌ |

---

### SQL-5：CTE 注释位置规范

**规范出处**：「CTE 注释块 - 位置规范」「CTE 注释块 - 格式规范 - 分隔符」章节。

**代码实际值提取规则**：

1. 扫描所有 `,cteN_xxx AS (` 模式，记录每个 CTE 的行号
2. **定位注释块**：向前遍历，找到最近的上方 `-- ====` 分隔符，记录注释块起止行号
3. **检查注释块与 AS 的行间距**：注释块结束行与 `AS (` 之间，不得有任何非空行以外的其他语句行（如 DELETE、INSERT、SELECT 等）；纯空行不计入间距
4. **检查分隔符完整性**：注释块上方必须有 `-- ====` 分隔符，不得只有单行 `-- CTE<序号>: ...` 而无上下分隔符

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 所有 CTE 注释块在 `AS (` 之前，且注释块结束行与 `AS (` 之间无其他语句行 | ✅ |
| 注释块在 `AS (` 括号内部 | ❌ |
| 注释块与 `AS (` 之间有 DELETE/INSERT 等语句行（行间距 ≥ 1 行非空语句行） | ❌ |
| 缺少 `-- ====` 上下分隔符 | ❌ |

---

### SQL-6：CTE 注释格式规范

**规范出处**：「CTE 注释块 - 格式规范」章节。

**代码实际值提取规则**：

1. **逐个 CTE 提取注释块**：以 `-- ====` 上分隔符为起点，向下找到下分隔符 `-- ====`，构成一个完整的注释块
2. **检查分隔符完整性**（高权重）：每个注释块必须同时有上下分隔符 `-- ====`，不得只有单行 `-- CTE<序号>: ...` 而无上下分隔符
3. **逐字段检查格式行**：`-- 字段名 <类型>，<格式> 示例：<sample> <含义>`，四要素（字段名、类型、格式、示例）缺一不可
4. **逐段落检查结构**：必须包含 `-- CTE<序号>：` 标识行、`-- 说明：` 段落（`1) 2) ...` 编号）、`-- 字段格式说明：` 段落
5. **检查空白分隔**：注释块结束后与下一行 `,cteN_xxx AS (` 之间应有空行

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 注释块格式完全符合正文规范要求 | ✅ |
| 缺少 `-- ====` 上下分隔符（高权重） | ❌ |
| 缺少 `-- CTE<序号>：` 标识行 | ❌ |
| 说明段落未使用 `1) 2) ...` 编号格式 | ❌ |
| 缺少 `-- 字段格式说明：` 段落 | ❌ |
| 字段格式行四要素不完整 | ❌ |
| 注释块结束后与 `AS (` 之间无空行 | ❌ |

---

### SQL-7：分区裁剪硬编码常量

**规范出处**：「分区裁剪规则（强制）」章节。

**代码实际值提取规则**：

1. 扫描所有 `WHERE ds =` 或 `AND ds =` 条件
2. 检查等号右侧是否为硬编码常量（`{prebizdate}` 或 `TO_CHAR(TO_DATE('{prebizdate}') ...` 表达式）
3. 检查是否存在 `WHERE ds = rds.xxx` 或 `WHERE ds = cte0.xxx`（引用 CTE 字段）
4. 日维场景：额外检查是否为范围条件（`BETWEEN ... AND ...` 或 `>= ... AND <= ...`）

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| 所有 `ds` 分区条件使用硬编码常量 | ✅ |
| 日维使用范围条件（`>= ... AND <= ...`）而非单值条件 | ✅ |
| 任一分区条件引用 CTE0/CTE 字段 | ❌ |

---

### SQL-8：INSERT 列列表字段格式

**规范出处**：「INSERT 书写规范（强制）」章节。

**代码实际值提取规则**：

1. 扫描所有 `INSERT INTO` 语句
2. 检查是否缺少列列表（`INSERT INTO <table>` 后直接跟 SELECT）
3. 检查列列表中是否每行只有一个字段
4. 统计是否存在任一行的 `INSERT INTO` 列名列表中包含多个逗号分隔的字段

**判定逻辑**：

| 场景 | 判定 |
|------|------|
| `INSERT INTO <table> (field1, field2, ...)` 显式列列表 + 每行一个字段 | ✅ |
| `INSERT INTO <table>` 无列列表，直接跟 SELECT | ❌（禁止省略列列表） |
| INSERT 列列表中出现 `,` 合并多字段 | ❌ |
