---
description: 调整开发 Workflow - 加载规范 → 读取 Skill → 字段修改 → 代码审查 → 返回结果
---

> **规则激活声明**：本 Workflow 全程激活以下规则，不可绕过：
> - `.cursor/rules/Datawarehouse/think.mdc`（思维范式，贯穿所有 Step）
>
> 本 Workflow 加载**全部知识资产**（业务知识 + schema 知识），不分阶段。

---

## Step 1: 加载规范

> 前提：无

读取以下规范文件：

```
规范:
  - .cursor/rules/Datawarehouse/sql-standards.mdc（SQL 通用代码开发规范）
  - .cursor/rules/Datawarehouse/{db}-standards.mdc（数据库特有规范，db 由目标表语法类型决定）
```

---

## Step 2: 读取修改 Skill

> 前提：Step 1 规范文件已读取

读取 `sql-modify-skill` skill。

```
skill: sql-modify-skill
封闭边界: Ticket 中指定的修改内容
```

---

## Step 3: 字段修改

> 前提：Step 2 sql-modify-skill 已读取

执行 `sql-modify-skill` skill。

```
输入上下文（来自 Ticket）:
  - layers（覆盖的层级）
  - 字段定义表格（指明要修改哪些字段及修改方式）
产物: 修改后的 SQL 代码
```

---

## Step 4: 代码审查

> 前提：Step 3 字段修改完成

```
返回 dispatcher → 重新分发至 code-review.mdc
```

---

## Step 5: 返回结果

> 前提：Step 4 代码审查通过

```
产物:
  - 最终 SQL 代码
  - Ticket status → done
```
