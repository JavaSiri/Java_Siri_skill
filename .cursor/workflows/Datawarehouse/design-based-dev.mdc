---
description: 全新设计新建开发 Workflow - 动态追问澄清 → 生成 Ticket → 加载规范 → 表结构设计 → ETL 开发 → 代码审查 → 返回结果
---

> **规则激活声明**：本 Workflow 全程激活以下规则，不可绕过：
> - `.cursor/rules/Datawarehouse/think.mdc`（思维范式，贯穿所有 Step）
>
> 本 Workflow 加载**全部知识资产**（业务知识 + schema 知识），不分阶段。

---

## Step 1: 需求澄清

> 前提：无

读取 `table-requirement-analyzer` skill，向用户动态生成追问，确认歧义点。

```
skill: table-requirement-analyzer
封闭边界: 用户提出的表需求
产物: 工作上下文中各维度的【澄清记录】，澄清收敛后进入 Step 2
```

---

## Step 2: 生成 Ticket

> 前提：Step 1 追问记录已在工作上下文中，所有歧义点已收敛

读取 `to_issues` skill，将澄清结论拆解为开发 Ticket。

```
skill: to_issues
封闭边界: Step 1 上下文中已收敛的澄清结论
产物: .cursor/tickets/ 目录下每个切片一个文件
```

---

## Step 3: 加载规范

> 前提：Step 2 Ticket 已生成

读取以下规范文件：

```
规范:
  - .cursor/rules/Datawarehouse/sql-standards.mdc（SQL 通用代码开发规范）
  - .cursor/rules/Datawarehouse/{db}-standards.mdc（数据库特有规范，db 由目标表语法类型决定）
  - .cursor/rules/Datawarehouse/naming-conventions.mdc（如有，表/字段命名规范）
```

---

## Step 4: 表结构设计

> 前提：Step 3 规范文件已读取

基于澄清结论和数仓分层规范，设计目标表的完整结构：

```
输入上下文（来自澄清结论）:
  - 目标表名
  - 字段定义（列名、类型、注释）
  - 主键 / 唯一键设计
  - 分区字段与分区策略
  - 是否拉链（缓慢变化维处理）
产物:
  - 目标表 DDL（create table 语句）
  - 字段说明文档
```

---

## Step 5: ETL 开发

> 前提：Step 4 DDL 已确认

读取 `report-builder` skill，执行 ETL 开发。

```
输入上下文（来自澄清结论）:
  - layers（目标层级）
  - 上游依赖表
  - 数据清洗规则
  - 增量 / 全量策略
产物: ETL SQL 代码
```

---

## Step 6: 代码审查

> 前提：Step 5 ETL 开发完成

```
返回 dispatcher → 重新分发至 code-review.mdc
```

---

## Step 7: 返回结果

> 前提：Step 6 代码审查通过

```
产物:
  - 最终 SQL 代码（DDL + ETL）
  - Ticket status → done
```
