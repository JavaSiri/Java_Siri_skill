---
description: 代码审查 Workflow - 加载规范 → 执行审查 → 输出报告 → 更新 Ticket 状态
---

> **规则激活声明**：本 Workflow 全程激活以下规则，不可绕过：
> - `.cursor/rules/Datawarehouse/think.mdc`（思维范式，贯穿所有 Step）
>
> 本 Workflow 加载**全部知识资产**（业务知识 + schema 知识），不分阶段。

---

## Step 1: 加载规范

读取以下规范文件：

```
规范:
  - .cursor/rules/Datawarehouse/sql-standards.mdc（SQL 通用代码开发规范 + 快速验收清单）
  - .cursor/rules/Datawarehouse/{db}-standards.mdc（数据库特有规范 + 快速验收清单，db 由代码语法类型决定）
skill: sql-code-review
```

---

## Step 2: 审查执行

> 前提：Step 1 读取清单已输出

执行 `sql-code-review` skill。

```
输入上下文（来自 Ticket 或用户请求）:
  - 审查范围（单个文件 / 多个文件 / PR 变更集）
产物: 审查报告
```

---

## Step 3: 返回审查结果

> 前提：Step 2 审查执行完成

```
产物:
  - 按 sql-code-review「审查报告格式」输出报告
  - 若审查通过 → 返回上游 Workflow 继续后续步骤
  - 若审查未通过 → Ticket status 保持不变，等待修复后重新提交
```
