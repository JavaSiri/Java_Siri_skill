---
description: 非报表开发前置流程 - 需求澄清 → 拆解 Ticket → 分发执行（适用于 ODS/DWD/DWS/ADS 层字段修改、新建表、新增 ETL 任务等非报表类需求）
---

> **规则激活声明**：本 Workflow 全程激活以下规则，不可绕过：
> - `.cursor/rules/Datawarehouse/think.mdc`（思维范式，贯穿所有 Step）
>
> **编排原则**：本文件是编排层，只声明执行顺序和激活规则。执行逻辑全部委托给 skill，不重抄 skill 内容。

---

## Step 1: 需求澄清

> 前提：无

读取 `requirement-analyzer` skill，对需求进行结构化澄清。

```
skill: requirement-analyzer
封闭边界: 用户需求文本
判断：目标表是否已存在？
  - 已存在 → 读取目标表 DDL 并纳入澄清上下文
  - 不存在 → 询问用户目标表所在层级（ODS/DWD/DWS/ADS）
产物: 需求澄清结论（含目标表、字段变更清单、数据链路说明）
```

---

## Step 2: 拆解 Ticket

> 前提：Step 1 需求澄清结论已输出

读取 `to_issues` skill，根据澄清结论拆解为独立可执行的 Ticket。

```
skill: to_issues
封闭边界: Step 1 的需求澄清结论
产物: .cursor/tickets/ 目录下每个切片一个文件
```

---

## Step 3: 分发执行

> 前提：Step 2 Ticket 列表已持久化

读取 `dispatcher` skill，按 task_type 分发到对应开发 workflow。

```
skill: dispatcher
封闭边界: .cursor/tickets/ 目录
分发规则:
  - 新建开发 → new-dev.mdc
  - 调整开发 → modify-dev.mdc
  - 转译开发 → translate-dev.mdc
产物: 进入对应开发 workflow
```
