---
description: 参考已有表新建开发 Workflow - 读取参考表 → AI 分析 → 动态追问 → 强制检查点 → 生成 Ticket → 加载规范 → 适配开发 → 代码审查 → 返回结果
---

> **规则激活声明**：本 Workflow 全程激活以下规则，不可绕过：
> - `.cursor/rules/Datawarehouse/think.mdc`（思维范式，贯穿所有 Step）
>
> 本 Workflow 加载**全部知识资产**（业务知识 + schema 知识），不分阶段。

---

## Step 1: 读取参考表

> 前提：用户指定了参考表名

读取参考表的 DDL 和最新 ETL 脚本：

```
输入（来自用户指定）:
  - 参考表名（reference_table）
产物:
  - 参考表 DDL（字段列表、注释、分区字段）
  - 参考表最近一版 ETL SQL
  - 参考表的血缘关系（上游表、下游报表）
```

---

## Step 2: AI 分析参考表

> 前提：Step 1 参考表已读取

AI 主动分析参考表代码和知识库表结构，建立对数据来源、关联关系、口径逻辑的理解，输出分析结论供后续追问使用。

```
AI 分析任务：
1. 读取参考表 DDL → 字段类型、注释、分区字段、主键
2. 读取参考表 ETL SQL → 数据来源表、数据清洗逻辑、关联方式（JOIN/UNION）
3. 读取知识库 schema/ 相关表结构 → 同主题域其他表的字段定义，识别共用字段和差异
4. 追溯血缘链路 → 上游 ODS/DWD 层有哪些表直接供给此表

产物：「AI 先验分析结论」（写入工作上下文）
```

「AI 先验分析结论」输出格式：

```
【AI 先验分析结论】
数据来源：
  - 直接来源表：{表名}（{CDC / 内部表 / 日志}）
  - 间接待来源：{表名列表}
  - 是否有 ODS 层支撑：{是 / 否}

关联关系：
  - JOIN 维度表：{表名}，关联键：{字段}
  - 事实表关联方式：{一对多 / 多对多}
  - 是否有潜在循环依赖：{是 / 否}

字段分析：
  - 关键业务字段：{字段列表}
  - 派生字段及其口径：{字段：口径描述}
  - 与同主题域其他表的共用字段：{字段列表}

待向用户确认的歧义点（由 AI 识别）：
  1. {歧义描述}
  2. {歧义描述}
```

---

## Step 3: 需求澄清

> 前提：Step 2 AI 分析已完成，形成「AI 先验分析结论」

读取 `table-requirement-analyzer` skill，基于 Step 2 的分析结论向用户动态生成追问。追问须引用「AI 先验分析结论」中的具体歧义点，不得从零提固定问题。

```
skill: table-requirement-analyzer
封闭边界: 用户提出的表需求 + 「AI 先验分析结论」
产物: 工作上下文中各维度的【澄清记录】，澄清收敛后进入 Step 4
```

---

## Step 4: 生成 Ticket

> 前提：Step 3 追问记录已在工作上下文中，所有歧义点已收敛

读取 `to_issues` skill，将澄清结论拆解为开发 Ticket。

```
skill: to_issues
封闭边界: Step 3 上下文中已收敛的澄清结论
产物: .cursor/tickets/ 目录下每个切片一个文件
```

---

## Step 5: 加载规范

> 前提：Step 4 Ticket 已生成

读取以下规范文件：

```
规范:
  - .cursor/rules/Datawarehouse/sql-standards.mdc（SQL 通用代码开发规范）
  - .cursor/rules/Datawarehouse/{db}-standards.mdc（数据库特有规范，db 由目标表语法类型决定）
```

---

## Step 6: 适配开发

> 前提：Step 5 规范文件已读取

读取 `report-builder` skill，执行基于参考表的适配开发。

```
输入上下文（来自 Ticket）:
  - 目标表名
  - 新增/调整的字段定义（与参考表的差异部分）
  - 业务规则变更（如口径与参考表不同）
  - layers（目标层级：ODS/DWD/DWS/ADS）
产物: 目标表 DDL + ETL SQL
```

---

## Step 7: 代码审查

> 前提：Step 6 适配开发完成

```
返回 dispatcher → 重新分发至 code-review.mdc
```

---

## Step 8: 返回结果

> 前提：Step 7 代码审查通过

```
产物:
  - 最终 SQL 代码（DDL + ETL）
  - Ticket status → done
```
